"use client";

/**
 * Legacy placeholder for pages that used a MUI AppBar.
 * Current project pages render their own headings, so this is intentionally empty.
 */
export default function ResponsiveAppBarPlaceholder() {
  return null;
}
