# Portfolio knowledge (RAG)

Markdown here is chunked, embedded with `text-embedding-3-small`, and written to `vectors.json` by:

```bash
npm run knowledge:embed
```

Requires `OPENAI_API_KEY` in the environment.

Add new case studies as `projects/your-case-study.md`, then re-run the command.
