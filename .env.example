# Copy to .env.local and fill in. Restart `npm run dev` after changes.

# Required for /api/chat → DeepSeek (server-only; never commit real values)
DEEPSEEK_API_KEY=

# Injected into JSON when the client sends `messages` but no `model`
MODEL_NAME=deepseek-chat

# ChatKit domain key from OpenAI (client + server registration)
NEXT_PUBLIC_CHATKIT_DOMAIN_KEY=

# Optional overrides (other OpenAI-compatible providers)
# LLM_API_KEY=
# LLM_CHAT_COMPLETIONS_URL=https://api.deepseek.com/v1/chat/completions
# LLM_BASE_URL=
